# Octopus (CockroachPet) 项目技术架构分析报告

## 1. 项目基本信息
- **项目名称**: Octopus (代码库: CockroachPet)
- **技术栈**: .NET 8.0 (Windows Forms), GDI+, Win32 API
- **定位**: 桌面趣味像素机器人 + 嵌入式 CMD 终端管理工具

## 2. 核心架构设计

项目采用典型的**事件驱动与状态机混合架构**，主要模块划分如下：

### 2.1 核心逻辑层 (Core Logic)
- **`Form1.cs` (主控窗体)**: 
  - 充当整个应用的“大脑”，管理 `Robot` 实例集合。
  - 维护系统托盘 (NotifyIcon) 和全局菜单。
  - 响应全局热键（如 ESC 菜单、F11 穿透模式、空格暂停）。
- **`Robot.cs` (实体模型)**: 
  - 封装机器人的物理状态（坐标、速度、缩放、颜色、名字）。
  - 实现基于向量反射的碰撞检测算法。

### 2.2 渲染表现层 (Rendering)
- **`PixelRobotRenderer.cs`**: 
  - 核心渲染引擎，使用 **GDI+** 直接在内存中绘制像素块。
  - 通过帧计数器控制动画序列（摆动、眨眼、晃动）。
  - 提供 6 种预设调色盘，实现机器人的个性化外观。

### 2.3 终端交互层 (Terminal Integration)
- **`TerminalManagerForm.cs`**: 
  - 管理真实的 Windows CMD 进程。
  - 使用 Win32 API (`SetWindowLong`, `SetParent`) 进行窗口嵌入与层级控制。
  - 实现命令管道重定向，支持在机器人的终端中执行系统级命令。

## 3. 关键技术实现点

### 3.1 高级窗口控制 (Win32 Interop)
为了实现“漂浮在桌面”的效果，项目重度使用了 P/Invoke 技术：
- **点击穿透**: 修改窗口扩展样式 `WS_EX_TRANSPARENT | WS_EX_LAYERED`，允许用户操作机器人下方的窗口。
- **透明处理**: 使用 `UpdateLayeredWindow` 或 `SetLayeredWindowAttributes` 实现非矩形窗口的视觉效果。
- **层级管理**: 强制机器人窗口位于 `HWND_TOPMOST`，确保其在所有普通程序之上。

### 3.2 动态像素渲染系统
- **无感缩放**: 所有的绘制指令均基于基础像素单元 (Unit Size)，支持 0.5x 到 3.0x 的平滑缩放而不失真。
- **性能优化**: 采用双缓冲技术 (Double Buffering) 减少移动和动画时的闪烁感。

### 3.3 进程与生命周期管理
- **CMD 后台持久化**: 终端窗口关闭后，底层的 CMD 进程并不会退出，而是隐藏在后台运行，确保执行中的复杂脚本任务不会中断。
- **配置持久化**: 使用本地 JSON 或 Settings 文件存储用户的个性化设置（名字、颜色、移动速度等）。

## 4. 扩展性分析

- **插件化可能**: `Robot` 类具有良好的封装，未来可以扩展不同的“机器人种类”或“行为包”。
- **跨平台限制**: 由于强依赖 Win32 API 和 WinForms，项目目前仅支持 Windows 平台。若需跨平台，核心渲染逻辑需迁移至 SkiaSharp。

## 5. 项目结构一览
```text
CockroachPet/
├── Program.cs              # 初始化与启动
├── Form1.cs                # 主控制器逻辑
├── Robot.cs                # 机器人逻辑实体
├── PixelRobotRenderer.cs   # 像素绘制引擎
├── TerminalManagerForm.cs  # CMD 终端管理
├── ControlPanelForm.cs     # 全局管理面板
├── SettingsForm.cs         # 偏好设置界面
└── Assets/                 # 辅助资源
```

---
*本报告由 Antigravity 自动生成，用于项目技术框架深度解析。*
