# 统一终端管理器说明

## 🎯 设计目标

参考 VS Code 的终端面板设计，使用标签页（Tab）统一管理所有机器人的终端，而不是每个机器人弹出一个独立窗口。

## ✨ 核心特性

### 1. 单例模式
- 全局只有一个终端管理器窗口
- 使用 `TerminalManagerForm.Instance` 访问

### 2. 标签页管理
- 每个机器人一个标签页
- 标签页显示机器人名称
- 点击标签页切换终端

### 3. 自动管理
- 打开终端：如果标签页已存在则切换，否则创建新标签页
- 关闭终端：移除对应的标签页
- 所有标签页关闭时，窗口自动隐藏

### 4. 简洁布局
- 只有标签页和终端区域
- 没有多余的标题栏和按钮
- 深色主题，专业外观

## 🏗️ 架构设计

### 类结构

```
TerminalManagerForm (单例)
├── TabControl (_tabControl)
│   ├── TabPage 1 (机器人 A)
│   │   └── TerminalTab
│   │       └── Panel (嵌入 CMD)
│   ├── TabPage 2 (机器人 B)
│   │   └── TerminalTab
│   │       └── Panel (嵌入 CMD)
│   └── ...
└── Dictionary<Robot, TerminalTab> (_terminals)
```

### TerminalManagerForm
- **职责**：管理所有终端标签页
- **单例模式**：全局唯一实例
- **方法**：
  - `OpenTerminal(Robot)` - 打开或切换到机器人的终端
  - `CloseTerminal(Robot)` - 关闭机器人的终端标签页

### TerminalTab
- **职责**：管理单个终端的 CMD 进程和嵌入
- **包含**：
  - TabPage - 标签页
  - Panel - 终端容器
  - Process - CMD 进程
  - IntPtr - CMD 窗口句柄

## 📋 使用方法

### 1. 打开终端

```csharp
// 方式 1：点击机器人
robot.OpenTerminal();

// 方式 2：控制面板双击
// 方式 3：控制面板右键菜单
```

**行为**：
- 如果终端管理器窗口未显示 → 显示窗口
- 如果该机器人的标签页不存在 → 创建新标签页
- 如果该机器人的标签页已存在 → 切换到该标签页
- 自动设置焦点到终端

### 2. 关闭终端

```csharp
robot.CloseTerminal();
```

**行为**：
- 移除该机器人的标签页
- 终止 CMD 进程
- 如果所有标签页都关闭 → 隐藏窗口

### 3. 切换终端

- 点击标签页切换
- 或再次调用 `OpenTerminal()` 切换

## 🎨 界面设计

### 窗口属性
- 大小：1000x700
- 最小尺寸：600x400
- 背景色：深灰色 `#1E1E1E`
- 居中显示

### 标签页样式
- 背景色：`#282828`
- 文字颜色：白色
- 字体：Consolas, 10pt
- 内边距：15x5

### 终端区域
- 背景色：黑色
- 无边框
- 填充整个标签页

## 🔄 工作流程

### 打开第一个终端
```
用户点击机器人 A
    ↓
调用 TerminalManagerForm.Instance.OpenTerminal(A)
    ↓
创建 TerminalTab(A)
    ↓
启动 CMD 进程
    ↓
嵌入 CMD 窗口到 Panel
    ↓
添加标签页到 TabControl
    ↓
显示终端管理器窗口
```

### 打开第二个终端
```
用户点击机器人 B
    ↓
调用 TerminalManagerForm.Instance.OpenTerminal(B)
    ↓
创建 TerminalTab(B)
    ↓
添加新标签页
    ↓
切换到新标签页
    ↓
窗口已显示，激活窗口
```

### 切换到已存在的终端
```
用户点击机器人 A（已打开）
    ↓
调用 TerminalManagerForm.Instance.OpenTerminal(A)
    ↓
检测到标签页已存在
    ↓
切换到该标签页
    ↓
设置焦点到终端
```

### 关闭终端
```
用户调用 CloseTerminal(A)
    ↓
获取 TerminalTab(A)
    ↓
终止 CMD 进程
    ↓
移除标签页
    ↓
从字典中删除
    ↓
如果没有标签页了 → 隐藏窗口
```

## 💡 关键技术点

### 1. 单例模式实现

```csharp
private static TerminalManagerForm? _instance;

public static TerminalManagerForm Instance
{
    get
    {
        if (_instance == null || _instance.IsDisposed)
        {
            _instance = new TerminalManagerForm();
        }
        return _instance;
    }
}
```

### 2. 标签页管理

```csharp
private Dictionary<Robot, TerminalTab> _terminals = new Dictionary<Robot, TerminalTab>();

public void OpenTerminal(Robot robot)
{
    if (_terminals.ContainsKey(robot))
    {
        // 已存在，切换
        _tabControl.SelectedTab = _terminals[robot].TabPage;
    }
    else
    {
        // 不存在，创建
        var tab = new TerminalTab(robot);
        _terminals[robot] = tab;
        _tabControl.TabPages.Add(tab.TabPage);
    }
}
```

### 3. 自动隐藏

```csharp
public void CloseTerminal(Robot robot)
{
    // ... 移除标签页 ...
    
    if (_terminals.Count == 0)
    {
        this.Hide();  // 没有标签页时隐藏窗口
    }
}
```

### 4. 阻止关闭

```csharp
private void TerminalManagerForm_FormClosing(object? sender, FormClosingEventArgs e)
{
    if (e.CloseReason == CloseReason.UserClosing)
    {
        e.Cancel = true;  // 取消关闭
        this.Hide();      // 改为隐藏
    }
}
```

## 🆚 对比：旧方案 vs 新方案

### 旧方案（独立窗口）
- ❌ 每个机器人一个独立窗口
- ❌ 窗口管理混乱
- ❌ 任务栏图标过多
- ❌ 需要手动管理每个窗口

### 新方案（统一管理）
- ✅ 所有终端在一个窗口
- ✅ 标签页切换，简洁高效
- ✅ 只有一个任务栏图标
- ✅ 自动管理，无需手动操作

## 🎯 用户体验

### 场景 1：管理多个机器人
```
1. 点击机器人 A → 打开终端管理器，显示 A 的终端
2. 点击机器人 B → 在同一窗口新增 B 的标签页
3. 点击机器人 C → 在同一窗口新增 C 的标签页
4. 点击标签页切换 → 快速切换不同机器人的终端
```

### 场景 2：关闭不需要的终端
```
1. 右键机器人 A → 选择"关闭终端"
2. A 的标签页被移除
3. 自动切换到其他标签页
4. 如果所有标签页都关闭 → 窗口自动隐藏
```

### 场景 3：重新打开终端
```
1. 点击机器人 A（之前关闭过）
2. 创建新的标签页
3. 启动新的 CMD 进程
4. 历史记录重新开始
```

## 📊 性能优化

### 1. 延迟加载
- CMD 进程在标签页创建时才启动
- 不是一次性启动所有终端

### 2. 资源释放
- 关闭标签页时立即终止 CMD 进程
- 释放窗口句柄和资源

### 3. 单例模式
- 避免重复创建管理器窗口
- 减少内存占用

## 🔮 未来扩展

### 1. 标签页右键菜单
- 关闭标签页
- 关闭其他标签页
- 关闭所有标签页
- 重命名标签页

### 2. 标签页拖拽
- 拖拽调整标签页顺序
- 拖拽标签页到新窗口

### 3. 分割视图
- 水平分割
- 垂直分割
- 同时查看多个终端

### 4. 快捷键支持
- Ctrl+Tab - 切换标签页
- Ctrl+W - 关闭当前标签页
- Ctrl+Shift+T - 重新打开关闭的标签页

## 🎉 总结

新的统一终端管理器设计：
- ✅ 简洁的标签页布局
- ✅ 统一的窗口管理
- ✅ 自动化的生命周期管理
- ✅ 类似 VS Code 的专业体验

完美实现了"所有终端在同一个容器，右侧新增标签页"的需求！
