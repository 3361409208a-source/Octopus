# 集成终端实现说明

## 🎯 设计目标

参考 Visual Studio Code 的终端集成方式，将 CMD 终端嵌入到程序窗口内部，而不是打开独立的 CMD 窗口。

## ✨ 核心特性

### 1. 终端嵌入窗口
- CMD 窗口作为子窗口嵌入到 Form 的 Panel 中
- 移除 CMD 窗口的标题栏和边框
- 终端完全填充面板区域

### 2. 自定义标题栏
- 显示机器人名称和 ID
- 自定义关闭按钮（X）
- 点击关闭按钮隐藏窗口而不是关闭进程

### 3. 窗口管理
- 可调整大小，终端自动适应
- 最小尺寸 400x300
- 默认尺寸 800x600
- 居中显示

### 4. 历史记录保留
- CMD 进程持续运行
- 隐藏窗口不会丢失命令历史
- 重新显示窗口时历史记录完整保留

## 🔧 技术实现

### Win32 API 使用

#### 1. SetParent - 设置父窗口
```csharp
SetParent(_cmdHwnd, _terminalPanel.Handle);
```
将 CMD 窗口设置为 Panel 的子窗口，实现嵌入效果。

#### 2. GetWindowLong/SetWindowLong - 修改窗口样式
```csharp
int style = GetWindowLong(_cmdHwnd, GWL_STYLE);
style &= ~WS_CAPTION;  // 移除标题栏
style &= ~WS_BORDER;   // 移除边框
style |= WS_CHILD;     // 设置为子窗口
SetWindowLong(_cmdHwnd, GWL_STYLE, style);
```

#### 3. MoveWindow - 调整窗口大小
```csharp
MoveWindow(_cmdHwnd, 0, 0, _terminalPanel.Width, _terminalPanel.Height, true);
```
让终端填充整个面板区域。

### 窗口结构

```
TerminalForm (主窗口)
├── _titleLabel (标题栏)
│   └── 显示: "🤖 {机器人名称} - Integrated Terminal"
├── _closeButton (关闭按钮)
│   └── 点击: 隐藏窗口
└── _terminalPanel (终端容器)
    └── CMD 窗口 (嵌入的子窗口)
```

### 事件处理

#### 1. Shown 事件
```csharp
private void TerminalForm_Shown(object? sender, EventArgs e)
{
    if (_cmdHwnd != IntPtr.Zero)
    {
        EmbedCmdWindow();  // 窗口显示后嵌入 CMD
    }
}
```

#### 2. Resize 事件
```csharp
private void TerminalForm_Resize(object? sender, EventArgs e)
{
    if (_cmdHwnd != IntPtr.Zero && _terminalPanel != null)
    {
        // 调整 CMD 窗口大小以适应面板
        MoveWindow(_cmdHwnd, 0, 0, _terminalPanel.Width, _terminalPanel.Height, true);
    }
}
```

#### 3. FormClosing 事件
```csharp
private void TerminalForm_FormClosing(object? sender, FormClosingEventArgs e)
{
    if (e.CloseReason == CloseReason.UserClosing)
    {
        e.Cancel = true;  // 取消关闭
        this.Hide();      // 改为隐藏
        return;
    }
    // 真正关闭时才终止 CMD 进程
}
```

## 🎨 界面设计

### 颜色方案
- 主窗口背景：`Color.FromArgb(30, 30, 30)` - 深灰色
- 标题栏背景：`Color.FromArgb(40, 40, 40)` - 稍浅的灰色
- 标题文字：`Color.Lime` - 亮绿色
- 终端面板：`Color.Black` - 黑色
- 关闭按钮悬停：`Color.Red` - 红色

### 字体
- 标题：Microsoft YaHei, 12pt, Bold
- 关闭按钮：Arial, 14pt, Bold

## 📋 使用方法

### 1. 打开终端
```csharp
robot.OpenTerminal();
```
- 如果终端不存在，创建新窗口并显示
- 如果终端已存在但隐藏，显示窗口

### 2. 隐藏终端
```csharp
robot.CloseTerminal();
// 或
robot.Terminal.HideTerminal();
```
- 隐藏窗口但保持 CMD 进程运行
- 命令历史记录保留

### 3. 通过控制面板
- 双击机器人 → 打开/显示终端
- 右键机器人 → 选择"隐藏终端"

### 4. 通过点击机器人
- 点击桌面上的机器人 → 打开/显示终端

## 🔄 工作流程

### 启动流程
1. 创建 TerminalForm 实例
2. 启动 CMD 进程
3. 等待 CMD 窗口创建（500ms）
4. 查找 CMD 窗口句柄
5. 窗口显示时嵌入 CMD
6. 移除 CMD 的标题栏和边框
7. 调整 CMD 大小以填充面板

### 显示/隐藏流程
```
显示: Form.Show() → CMD 窗口自动显示（子窗口）
隐藏: Form.Hide() → CMD 窗口自动隐藏（子窗口）
关闭: 用户点击 X → 拦截 → 改为隐藏
```

### 进程生命周期
```
创建终端 → 启动 CMD 进程 → 嵌入窗口
    ↓
显示/隐藏窗口（进程持续运行）
    ↓
退出主程序 → 终止 CMD 进程
```

## ✅ 优势对比

### 之前的方案（独立 CMD 窗口）
- ❌ CMD 窗口独立显示，不受程序控制
- ❌ 需要复杂的窗口消息拦截
- ❌ 用户可能意外关闭窗口
- ❌ 窗口管理混乱

### 当前方案（集成终端）
- ✅ 终端嵌入程序窗口，完全受控
- ✅ 简洁的 Win32 API 调用
- ✅ 用户无法意外关闭（只能隐藏）
- ✅ 统一的窗口管理
- ✅ 类似 VS Code 的专业体验

## 🎯 与 VS Code 的对比

### 相似之处
- ✅ 终端嵌入主窗口
- ✅ 自定义标题栏
- ✅ 可调整大小
- ✅ 关闭按钮隐藏而不是关闭

### 差异
- VS Code 使用 Electron + xterm.js（Web 技术）
- 我们使用 WinForms + 嵌入原生 CMD（Win32 API）
- VS Code 支持多个终端标签页
- 我们每个机器人一个独立终端窗口

## 🚀 未来改进方向

### 1. 多标签页支持
- 在一个窗口中管理多个机器人的终端
- 使用 TabControl 切换不同终端

### 2. 终端主题
- 支持自定义颜色方案
- 字体大小调整
- 透明度设置

### 3. 快捷键支持
- Ctrl+C 复制
- Ctrl+V 粘贴
- Ctrl+W 关闭终端

### 4. 终端分割
- 水平/垂直分割
- 同时查看多个终端

## 📝 测试步骤

1. 运行程序
2. 点击机器人打开终端
3. 观察：
   - ✅ 终端嵌入在窗口内部
   - ✅ 没有独立的 CMD 窗口标题栏
   - ✅ 自定义标题栏显示机器人信息
4. 输入命令测试：
   ```cmd
   echo test
   dir
   robot-name
   robot-status
   ```
5. 调整窗口大小 → 终端自动适应
6. 点击 X 按钮 → 窗口隐藏
7. 再次点击机器人 → 窗口重新显示
8. 按 ↑ 键 → 历史记录保留

## 🎉 总结

成功实现了类似 VS Code 的集成终端体验：
- ✅ 终端完全嵌入程序窗口
- ✅ 自定义界面和控制
- ✅ 命令历史记录保留
- ✅ 专业的用户体验
