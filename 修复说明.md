# 修复说明

## 已修复的问题

### 1. 终端关闭后机器人停止移动且无法再次打开
**问题原因：**
- 点击关闭按钮时，终端窗口被隐藏到托盘，但 `_robot.Terminal` 引用仍然存在
- 再次点击机器人时，`OpenTerminal()` 检测到 `Terminal != null` 就不会创建新窗口
- 机器人在打开终端时被设置为 `IsMoving = false`，关闭时没有恢复

**修复方案：**
- 修改 `Robot.OpenTerminal()` 方法，检测终端是否可见，如果隐藏则恢复显示
- 修改 `TerminalForm_FormClosing()` 方法，隐藏到托盘时立即恢复机器人移动
- 修改 `Form1_MouseClick()` 方法，点击机器人时不再停止移动

### 2. 每个机器人都有独立的设置面板
**问题原因：**
- 原代码中每次打开设置都创建新的 `SettingsForm` 实例
- `SettingsForm` 有独立的托盘图标，导致混乱

**修复方案：**
- 在 `Form1` 中添加 `_settingsForm` 单例字段
- 打开设置时检查是否已存在实例，存在则激活，不存在则创建
- 移除 `SettingsForm` 的托盘图标和最小化到托盘功能
- 简化 `SettingsForm` 为普通对话框

## 修改的文件

1. **Robot.cs**
   - 修改 `OpenTerminal()` 方法，支持恢复隐藏的终端窗口

2. **TerminalForm.cs**
   - 修改 `TerminalForm_FormClosing()` 方法，隐藏时恢复机器人移动

3. **Form1.cs**
   - 添加 `_settingsForm` 单例字段
   - 修改右键菜单中的设置选项，使用单例模式
   - 修改 `Form1_MouseClick()` 方法，点击时不停止机器人移动
   - 更新快捷键说明文本

4. **SettingsForm.cs**
   - 完全重写，移除托盘图标功能
   - 移除最小化到托盘功能
   - 简化为普通对话框

## 使用说明

### 终端操作
- **点击机器人** - 打开该机器人的CMD终端（机器人继续移动）
- **点击 X 按钮** - 隐藏终端到托盘（机器人恢复移动）
- **ESC 键** - 隐藏终端到托盘
- **双击托盘图标** - 恢复终端窗口
- **CMD中输入 exit** - 真正关闭终端

### 设置面板
- **右键托盘图标 → 设置** - 打开全局设置面板
- 只有一个设置面板实例，多次点击会激活已存在的窗口
- 设置会自动保存到临时文件

### 快捷键
- **ESC** - 打开右键菜单
- **F11** - 切换点击穿透模式
- **空格** - 暂停/继续所有机器人

## 测试建议

1. 启动程序，投放一个机器人
2. 点击机器人打开终端，确认机器人继续移动
3. 点击终端的 X 按钮，确认终端隐藏且机器人继续移动
4. 再次点击机器人，确认终端窗口恢复显示
5. 右键托盘图标打开设置，修改设置后关闭
6. 再次右键打开设置，确认只有一个设置窗口
