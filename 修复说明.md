# CMD 终端控制说明

## ✅ 最新实现（完全由程序控制）

### 核心特性
1. **移除窗口控制按钮** - CMD 窗口的最小化、最大化、关闭按钮已被禁用
2. **完全程序控制** - 只能通过点击机器人来显示/隐藏终端
3. **保持进程运行** - CMD 进程一直在后台运行，历史记录完整保留
4. **隐藏而非最小化** - 窗口使用 SW_HIDE 完全隐藏，不出现在任务栏

### 技术实现

#### 1. 禁用窗口控制按钮
```csharp
// 删除系统菜单中的控制项
IntPtr hMenu = GetSystemMenu(_cmdHwnd, false);
DeleteMenu(hMenu, SC_CLOSE, MF_BYCOMMAND);      // 删除关闭
DeleteMenu(hMenu, SC_MINIMIZE, MF_BYCOMMAND);   // 删除最小化
DeleteMenu(hMenu, SC_MAXIMIZE, MF_BYCOMMAND);   // 删除最大化

// 移除窗口样式
int style = GetWindowLong(_cmdHwnd, GWL_STYLE);
style &= ~WS_MINIMIZEBOX;  // 移除最小化按钮
style &= ~WS_MAXIMIZEBOX;  // 移除最大化按钮
SetWindowLong(_cmdHwnd, GWL_STYLE, style);
```

#### 2. 窗口显示/隐藏控制
```csharp
// 显示窗口
ShowWindow(_cmdHwnd, SW_SHOW);
SetForegroundWindow(_cmdHwnd);

// 隐藏窗口（完全隐藏，不在任务栏）
ShowWindow(_cmdHwnd, SW_HIDE);
```

### 用户体验

1. **打开终端**
   - 点击机器人 → CMD 窗口显示
   - 窗口标题栏只有标题，没有最小化/最大化/关闭按钮
   - 可以正常输入命令

2. **隐藏终端**
   - 再次点击机器人 → CMD 窗口隐藏
   - 或在控制面板右键选择"隐藏终端"
   - 窗口完全消失，不在任务栏显示

3. **历史记录保留**
   - CMD 进程一直运行
   - 所有命令历史保存在内存中
   - 重新显示窗口后，按 ↑ 键可以看到之前的命令

4. **无法手动关闭**
   - 用户无法通过点击 X 按钮关闭窗口（按钮已被移除）
   - 只能通过程序控制显示/隐藏
   - 退出主程序时，CMD 进程才会被终止

### 控制方式

#### 方式 1：点击机器人
- 点击桌面上的机器人 → 显示终端
- 再次点击 → 隐藏终端（切换显示状态）

#### 方式 2：控制面板
- 打开控制面板
- 双击机器人 → 显示终端
- 右键机器人 → 选择"隐藏终端"

### 测试步骤

1. 运行程序，点击机器人打开终端
2. 观察窗口：**应该没有最小化、最大化、关闭按钮**
3. 在终端输入命令：
   ```cmd
   echo test1
   echo test2
   dir
   ```
4. 按 ↑ 键确认历史记录存在
5. 再次点击机器人（窗口应该隐藏）
6. 再次点击机器人（窗口重新显示）
7. 按 ↑ 键 → **历史记录应该还在**

### 技术优势

1. **完全控制** - 用户无法意外关闭窗口
2. **简洁界面** - 窗口只有标题栏，没有多余按钮
3. **可靠性高** - 不依赖窗口消息拦截，直接修改窗口样式
4. **历史保留** - 进程持续运行，历史记录完整保存

### Windows API 说明

- `GetSystemMenu` - 获取窗口系统菜单
- `DeleteMenu` - 删除菜单项（关闭、最小化、最大化）
- `GetWindowLong/SetWindowLong` - 修改窗口样式
- `ShowWindow` - 控制窗口显示状态
- `SetForegroundWindow` - 激活窗口

### 为什么这个方案更好

**之前的方案问题**：
- ❌ Window Hook 不可靠
- ❌ 最小化到任务栏仍然可见
- ❌ 用户可以手动关闭窗口

**当前方案优势**：
- ✅ 直接修改窗口样式，移除控制按钮
- ✅ 完全隐藏，不在任务栏显示
- ✅ 用户无法手动关闭，只能通过程序控制
- ✅ 代码简单可靠，不依赖复杂的消息钩子

## 🎉 结论

当前实现完全满足需求：
- ✅ CMD 窗口由程序完全控制
- ✅ 用户无法手动关闭或最小化
- ✅ 只能通过点击机器人来显示/隐藏
- ✅ 命令历史记录完整保留
- ✅ 窗口界面简洁，没有多余按钮

